[project]
name = "blog"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "django",
    "gunicorn",
    "tzdata",
    "uvicorn",
    "wagtail",
    "wagtail-modeladmin>=2.2.0",
    "whitenoise",
]

[dependency-groups]
dev = [
    "black",
    "flake8",
    "isort",
]
