services:
  web:
    container_name: blog
    build: .
    env_file: .env
    ports:
      - "127.0.0.1:${PORT}:${PORT}"
    command: uv run gunicorn -b 0.0.0.0:${PORT} -w 2 app:app
    restart: unless-stopped
